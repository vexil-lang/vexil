// Code generated by vexilc. DO NOT EDIT.
// Source: test.concrete_type_aliases

use std::collections::BTreeMap;

pub const SCHEMA_HASH: [u8; 32] = [0x34, 0xf7, 0x2b, 0xbd, 0x77, 0x6a, 0x0f, 0x2d, 0xcb, 0x4a, 0xae, 0xea, 0xf9, 0xe3, 0xbc, 0x15, 0xed, 0x6b, 0xa6, 0x81, 0xac, 0x4d, 0x35, 0xbe, 0x2f, 0xf5, 0xa3, 0x4d, 0x8f, 0x0e, 0xb2, 0x34];

// ── Item ──
#[derive(Debug, Clone, PartialEq)]
pub struct Item {
    pub id: u32,
    pub _unknown: Vec<u8>,
}

impl vexil_runtime::Pack for Item {
    fn pack(&self, w: &mut vexil_runtime::BitWriter) -> Result<(), vexil_runtime::EncodeError> {
        w.write_u32(self.id);
        w.flush_to_byte_boundary();
        if !self._unknown.is_empty() {
            w.write_raw_bytes(&self._unknown);
        }
        Ok(())
    }
}

impl vexil_runtime::Unpack for Item {
    fn unpack(r: &mut vexil_runtime::BitReader<'_>) -> Result<Self, vexil_runtime::DecodeError> {
        let id = r.read_u32()?;
        r.flush_to_byte_boundary();
        let _unknown = Vec::new();
        Ok(Self {
            id,
            _unknown,
        })
    }
}


// ── AliasUser ──
#[derive(Debug, Clone, PartialEq)]
pub struct AliasUser {
    pub count: u32,
    pub label: String,
    pub direction: vexil_runtime::Vec3<f32>,
    pub permissions: u8,
    pub item: Item,
    pub names: Vec<String>,
    pub grid: [u16; 4],
    pub lookup: BTreeMap<String, Option<u32>>,
    pub tags: std::collections::BTreeSet<String>,
    pub outcome: Result<Vec<String>, String>,
    pub _unknown: Vec<u8>,
}

impl vexil_runtime::Pack for AliasUser {
    fn pack(&self, w: &mut vexil_runtime::BitWriter) -> Result<(), vexil_runtime::EncodeError> {
        w.write_u32(self.count);
        w.write_string(self.label.as_str());
        for c in [&self.direction.x, &self.direction.y, &self.direction.z] {
            w.write_f32(*c);
        }
        w.write_bits(self.permissions as u64, 2_u8);
        w.enter_recursive()?;
        self.item.pack(w)?;
        w.leave_recursive();
        w.write_leb128(self.names.len() as u64);
        for item in self.names.iter() {
            w.write_string(item.as_str());
        }
        for item in self.grid.iter() {
            w.write_u16(*item);
        }
        w.write_leb128(self.lookup.len() as u64);
        for (map_k, map_v) in self.lookup.iter() {
            w.write_string(map_k.as_str());
            w.write_bool(map_v.is_some());
            if let Some(ref inner_val) = map_v {
                w.flush_to_byte_boundary();
                w.write_u32(*inner_val);
            }
        }
        w.write_leb128(self.tags.len() as u64);
        for item in self.tags.iter() {
            w.write_string(item.as_str());
        }
        match &self.outcome {
            Ok(ok_val) => {
                w.write_bool(true);
                w.write_leb128(ok_val.len() as u64);
                for item in ok_val.iter() {
                    w.write_string(item.as_str());
                }
            }
            Err(err_val) => {
                w.write_bool(false);
                w.write_string(err_val.as_str());
            }
        }
        w.flush_to_byte_boundary();
        if !self._unknown.is_empty() {
            w.write_raw_bytes(&self._unknown);
        }
        Ok(())
    }
}

impl vexil_runtime::Unpack for AliasUser {
    fn unpack(r: &mut vexil_runtime::BitReader<'_>) -> Result<Self, vexil_runtime::DecodeError> {
        let count = r.read_u32()?;
        let label = r.read_string()?;
        let direction_x = r.read_f32()?;
        let direction_y = r.read_f32()?;
        let direction_z = r.read_f32()?;
        let direction = vexil_runtime::Vec3::<f32> { x: direction_x, y: direction_y, z: direction_z };
        let permissions = r.read_bits(2_u8)? as u8;
        r.enter_recursive()?;
        let item = vexil_runtime::Unpack::unpack(r)?;
        r.leave_recursive();
        let names_len = r.read_leb128(10_u8)? as usize;
        let mut names = Vec::with_capacity(names_len);
        for _ in 0..names_len {
            let names_item = r.read_string()?;
            names.push(names_item);
        }
        let mut grid_vec = Vec::with_capacity(4_usize);
        for _ in 0..4_usize {
            let grid_item = r.read_u16()?;
            grid_vec.push(grid_item);
        }
        let grid: [u16; 4] = grid_vec.try_into().map_err(|_| vexil_runtime::DecodeError::UnexpectedEof)?;
        let lookup_len = r.read_leb128(10_u8)? as usize;
        let mut lookup = std::collections::BTreeMap::new();
        for _ in 0..lookup_len {
            let lookup_k = r.read_string()?;
            let lookup_v_present = r.read_bool()?;
            let lookup_v = if lookup_v_present {
                r.flush_to_byte_boundary();
                let lookup_v_inner = r.read_u32()?;
                Some(lookup_v_inner)
            }
            else {
                None
            }
;
            lookup.insert(lookup_k, lookup_v);
        }
        let tags_len = r.read_leb128(10_u8)? as usize;
        let mut tags = std::collections::BTreeSet::new();
        for _ in 0..tags_len {
            let tags_item = r.read_string()?;
            tags.insert(tags_item);
        }
        let outcome_is_ok = r.read_bool()?;
        let outcome = if outcome_is_ok {
            let outcome_ok_len = r.read_leb128(10_u8)? as usize;
            let mut outcome_ok = Vec::with_capacity(outcome_ok_len);
            for _ in 0..outcome_ok_len {
                let outcome_ok_item = r.read_string()?;
                outcome_ok.push(outcome_ok_item);
            }
            Ok(outcome_ok)
        }
        else {
            let outcome_err = r.read_string()?;
            Err(outcome_err)
        }
;
        r.flush_to_byte_boundary();
        let _unknown = Vec::new();
        Ok(Self {
            count,
            label,
            direction,
            permissions,
            item,
            names,
            grid,
            lookup,
            tags,
            outcome,
            _unknown,
        })
    }
}
