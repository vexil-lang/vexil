[package]
name = "vexil-bench"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
vexil-runtime = { path = "../vexil-runtime" }

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "encode_decode"
harness = false
