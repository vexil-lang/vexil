// Code generated by vexilc. DO NOT EDIT.
// Source: test.recursive

import { BitReader, BitWriter } from '@vexil/runtime';

export const SCHEMA_HASH = new Uint8Array([0xa1, 0x25, 0xcb, 0x50, 0x29, 0x3e, 0xa3, 0x25, 0x60, 0x73, 0x08, 0x42, 0x52, 0xc5, 0xca, 0xf0, 0x83, 0xf5, 0xdd, 0xf7, 0x30, 0x18, 0xf8, 0x51, 0x8e, 0x54, 0xa9, 0xfd, 0xbe, 0xeb, 0x91, 0xae]);

// ── TreeNode ──
export interface TreeNode {
  value: number;
  children: TreeNode[];
  _unknown: Uint8Array;
}

export function encodeTreeNode(v: TreeNode, w: BitWriter): void {
  w.writeI32(v.value);
  w.writeLeb128(v.children.length);
  for (const item of v.children) {
    w.enterNested();
    encodeTreeNode(item, w);
    w.leaveNested();
  }
  w.flushToByteBoundary();
  if (v._unknown.length > 0) {
    w.writeRawBytes(v._unknown);
  }
}

export function decodeTreeNode(r: BitReader): TreeNode {
  const value = r.readI32();
  const children_len = r.readLeb128();
  const children: TreeNode[] = [];
  for (let i = 0; i < children_len; i++) {
    r.enterNested();
    const children_item = decodeTreeNode(r);
    r.leaveNested();
    children.push(children_item);
  }
  r.flushToByteBoundary();
  const _unknown = r.readRemaining();
  return { value, children, _unknown };
}


// ── LinkedList ──
export interface LinkedList {
  value: string;
  next: LinkedList | null;
  _unknown: Uint8Array;
}

export function encodeLinkedList(v: LinkedList, w: BitWriter): void {
  w.writeString(v.value);
  w.writeBool(v.next !== null);
  w.flushToByteBoundary();
  if (v.next !== null) {
    w.enterNested();
    encodeLinkedList(v.next, w);
    w.leaveNested();
  }
  w.flushToByteBoundary();
  if (v._unknown.length > 0) {
    w.writeRawBytes(v._unknown);
  }
}

export function decodeLinkedList(r: BitReader): LinkedList {
  const value = r.readString();
  const next_present = r.readBool();
  r.flushToByteBoundary();
  let next: LinkedList | null;
  if (next_present) {
    r.enterNested();
    const next_inner = decodeLinkedList(r);
    r.leaveNested();
    next = next_inner;
  } else {
    next = null;
  }
  r.flushToByteBoundary();
  const _unknown = r.readRemaining();
  return { value, next, _unknown };
}


// ── Expr ──
export interface Expr {
  kind: ExprKind;
  _unknown: Uint8Array;
}

export function encodeExpr(v: Expr, w: BitWriter): void {
  w.enterNested();
  encodeExprKind(v.kind, w);
  w.leaveNested();
  w.flushToByteBoundary();
  if (v._unknown.length > 0) {
    w.writeRawBytes(v._unknown);
  }
}

export function decodeExpr(r: BitReader): Expr {
  r.enterNested();
  const kind = decodeExprKind(r);
  r.leaveNested();
  r.flushToByteBoundary();
  const _unknown = r.readRemaining();
  return { kind, _unknown };
}


// ── ExprKind ──
export interface ExprKind_Literal {
  tag: 'Literal';
  value: bigint;
}

export interface ExprKind_Binary {
  tag: 'Binary';
  left: Expr;
  op: number;
  right: Expr;
}

export type ExprKind = ExprKind_Literal | ExprKind_Binary;

export function encodeExprKind(v: ExprKind, w: BitWriter): void {
  w.flushToByteBoundary();
  switch (v.tag) {
    case 'Literal': {
      w.writeLeb128(0);
      const payloadW = new BitWriter();
      payloadW.writeI64(v.value);
      payloadW.flushToByteBoundary();
      const payload = payloadW.finish();
      w.writeLeb128(payload.length);
      w.writeRawBytes(payload);
      break;
    }
    case 'Binary': {
      w.writeLeb128(1);
      const payloadW = new BitWriter();
      payloadW.enterNested();
      encodeExpr(v.left, payloadW);
      payloadW.leaveNested();
      payloadW.writeU8(v.op);
      payloadW.enterNested();
      encodeExpr(v.right, payloadW);
      payloadW.leaveNested();
      payloadW.flushToByteBoundary();
      const payload = payloadW.finish();
      w.writeLeb128(payload.length);
      w.writeRawBytes(payload);
      break;
    }
  }
}

export function decodeExprKind(r: BitReader): ExprKind {
  r.flushToByteBoundary();
  const disc = r.readLeb128();
  const len = r.readLeb128();
  switch (disc) {
    case 0: {
      const payloadBytes = r.readRawBytes(len);
      const pr = new BitReader(payloadBytes);
      const value = pr.readI64();
      pr.flushToByteBoundary();
      return { tag: 'Literal' as const, value };
    }
    case 1: {
      const payloadBytes = r.readRawBytes(len);
      const pr = new BitReader(payloadBytes);
      pr.enterNested();
      const left = decodeExpr(pr);
      pr.leaveNested();
      const op = pr.readU8();
      pr.enterNested();
      const right = decodeExpr(pr);
      pr.leaveNested();
      pr.flushToByteBoundary();
      return { tag: 'Binary' as const, left, op, right };
    }
    default: {
      throw new Error(`Unknown ExprKind discriminant: ${disc}`);
    }
  }
}

