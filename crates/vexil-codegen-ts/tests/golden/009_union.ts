// Code generated by vexilc. DO NOT EDIT.
// Source: test.unions

import { BitReader, BitWriter } from '@vexil-lang/runtime';

export const SCHEMA_HASH = new Uint8Array([0x2f, 0x63, 0x8f, 0x3e, 0x17, 0x8f, 0x50, 0x9c, 0x8f, 0x7b, 0xfb, 0x6d, 0x38, 0xa8, 0x56, 0x8f, 0x6e, 0x8c, 0xc9, 0xad, 0xe3, 0xf0, 0xa5, 0x06, 0x9a, 0x5c, 0x9d, 0x97, 0x97, 0x71, 0xa7, 0xb9]);

// ── Shape ──
export interface Shape_Circle {
  tag: 'Circle';
  radius: number;
}

export interface Shape_Rectangle {
  tag: 'Rectangle';
  width: number;
  height: number;
}

export interface Shape_Point {
  tag: 'Point';
}

export type Shape = Shape_Circle | Shape_Rectangle | Shape_Point;

export function encodeShape(v: Shape, w: BitWriter): void {
  switch (v.tag) {
    case 'Circle': {
      w.writeLeb128(0);
      const payloadW = new BitWriter();
      payloadW.writeF32(v.radius);
      payloadW.flushToByteBoundary();
      const payload = payloadW.finish();
      w.writeLeb128(payload.length);
      w.writeRawBytes(payload);
      break;
    }
    case 'Rectangle': {
      w.writeLeb128(1);
      const payloadW = new BitWriter();
      payloadW.writeF32(v.width);
      payloadW.writeF32(v.height);
      payloadW.flushToByteBoundary();
      const payload = payloadW.finish();
      w.writeLeb128(payload.length);
      w.writeRawBytes(payload);
      break;
    }
    case 'Point': {
      w.writeLeb128(2);
      w.writeLeb128(0);
      break;
    }
  }
}

export function decodeShape(r: BitReader): Shape {
  r.flushToByteBoundary();
  const disc = r.readLeb128();
  const len = r.readLeb128();
  if (len > 67_108_864) {
    throw new Error(`Shape payload length ${len} exceeds limit 67108864`);
  }
  switch (disc) {
    case 0: {
      const payloadBytes = r.readRawBytes(len);
      const pr = new BitReader(payloadBytes);
      const _vexilField0 = pr.readF32();
      pr.flushToByteBoundary();
      return { tag: 'Circle' as const, radius: _vexilField0 };
    }
    case 1: {
      const payloadBytes = r.readRawBytes(len);
      const pr = new BitReader(payloadBytes);
      const _vexilField0 = pr.readF32();
      const _vexilField1 = pr.readF32();
      pr.flushToByteBoundary();
      return { tag: 'Rectangle' as const, width: _vexilField0, height: _vexilField1 };
    }
    case 2: {
      r.readRawBytes(len);
      return { tag: 'Point' as const };
    }
    default: {
      r.readRawBytes(len);
      throw new Error(`Unknown Shape discriminant: ${disc}`);
    }
  }
}


// ── Color ──
export interface Color_Ansi {
  tag: 'Ansi';
  code: number;
}

export interface Color_Rgb {
  tag: 'Rgb';
  r: number;
  g: number;
  b: number;
}

export interface Color_Reset {
  tag: 'Reset';
}

export interface Color__VexilUnknown {
  tag: '__vexil_unknown';
  discriminant: number;
  data: Uint8Array;
}

export type Color = Color_Ansi | Color_Rgb | Color_Reset | Color__VexilUnknown;

export function encodeColor(v: Color, w: BitWriter): void {
  switch (v.tag) {
    case 'Ansi': {
      w.writeLeb128(0);
      const payloadW = new BitWriter();
      payloadW.writeU8(v.code);
      payloadW.flushToByteBoundary();
      const payload = payloadW.finish();
      w.writeLeb128(payload.length);
      w.writeRawBytes(payload);
      break;
    }
    case 'Rgb': {
      w.writeLeb128(1);
      const payloadW = new BitWriter();
      payloadW.writeU8(v.r);
      payloadW.writeU8(v.g);
      payloadW.writeU8(v.b);
      payloadW.flushToByteBoundary();
      const payload = payloadW.finish();
      w.writeLeb128(payload.length);
      w.writeRawBytes(payload);
      break;
    }
    case 'Reset': {
      w.writeLeb128(2);
      w.writeLeb128(0);
      break;
    }
    case '__vexil_unknown': {
      if (v.data.length > 67_108_864) {
        throw new Error(`Color payload length ${v.data.length} exceeds limit 67108864`);
      }
      w.writeLeb128(v.discriminant);
      w.writeLeb128(v.data.length);
      w.writeRawBytes(v.data);
      break;
    }
  }
}

export function decodeColor(r: BitReader): Color {
  r.flushToByteBoundary();
  const disc = r.readLeb128();
  const len = r.readLeb128();
  if (len > 67_108_864) {
    throw new Error(`Color payload length ${len} exceeds limit 67108864`);
  }
  switch (disc) {
    case 0: {
      const payloadBytes = r.readRawBytes(len);
      const pr = new BitReader(payloadBytes);
      const _vexilField0 = pr.readU8();
      pr.flushToByteBoundary();
      return { tag: 'Ansi' as const, code: _vexilField0 };
    }
    case 1: {
      const payloadBytes = r.readRawBytes(len);
      const pr = new BitReader(payloadBytes);
      const _vexilField0 = pr.readU8();
      const _vexilField1 = pr.readU8();
      const _vexilField2 = pr.readU8();
      pr.flushToByteBoundary();
      return { tag: 'Rgb' as const, r: _vexilField0, g: _vexilField1, b: _vexilField2 };
    }
    case 2: {
      r.readRawBytes(len);
      return { tag: 'Reset' as const };
    }
    default: {
      const data = r.readRawBytes(len);
      return { tag: '__vexil_unknown' as const, discriminant: disc, data };
    }
  }
}


// ── Event ──
export interface Event_Click {
  tag: 'Click';
  x: number;
  y: number;
}

export interface Event_Scroll {
  tag: 'Scroll';
  delta: number;
}

export type Event = Event_Click | Event_Scroll;

export function encodeEvent(v: Event, w: BitWriter): void {
  switch (v.tag) {
    case 'Click': {
      w.writeLeb128(0);
      const payloadW = new BitWriter();
      payloadW.writeU16(v.x);
      payloadW.writeU16(v.y);
      payloadW.flushToByteBoundary();
      const payload = payloadW.finish();
      w.writeLeb128(payload.length);
      w.writeRawBytes(payload);
      break;
    }
    case 'Scroll': {
      w.writeLeb128(2);
      const payloadW = new BitWriter();
      payloadW.writeI16(v.delta);
      payloadW.flushToByteBoundary();
      const payload = payloadW.finish();
      w.writeLeb128(payload.length);
      w.writeRawBytes(payload);
      break;
    }
  }
}

export function decodeEvent(r: BitReader): Event {
  r.flushToByteBoundary();
  const disc = r.readLeb128();
  const len = r.readLeb128();
  if (len > 67_108_864) {
    throw new Error(`Event payload length ${len} exceeds limit 67108864`);
  }
  switch (disc) {
    case 0: {
      const payloadBytes = r.readRawBytes(len);
      const pr = new BitReader(payloadBytes);
      const _vexilField0 = pr.readU16();
      const _vexilField1 = pr.readU16();
      pr.flushToByteBoundary();
      return { tag: 'Click' as const, x: _vexilField0, y: _vexilField1 };
    }
    case 2: {
      const payloadBytes = r.readRawBytes(len);
      const pr = new BitReader(payloadBytes);
      const _vexilField0 = pr.readI16();
      pr.flushToByteBoundary();
      return { tag: 'Scroll' as const, delta: _vexilField0 };
    }
    default: {
      r.readRawBytes(len);
      throw new Error(`Unknown Event discriminant: ${disc}`);
    }
  }
}
