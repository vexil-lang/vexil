# Sensor Telemetry Moved

The original sensor page has been replaced by the guided
[Quickstart](./quickstart.md). The new example retains the compact telemetry
schema while adding exact output, regeneration checks, and a runnable round
trip.
