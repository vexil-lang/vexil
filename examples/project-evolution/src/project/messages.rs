// Code generated by vexilc. DO NOT EDIT.
// Source: project.messages

use crate::project::types::Permissions;
use crate::project::types::Priority;
use crate::project::types::TaskState;
use crate::project::types::Timestamp;

pub const SCHEMA_HASH: [u8; 32] = [0xc6, 0x59, 0xd4, 0xd5, 0x44, 0xfa, 0x92, 0x1c, 0x96, 0x3b, 0xe1, 0xaf, 0x50, 0x1e, 0xf0, 0xac, 0xa3, 0x1c, 0x3f, 0x67, 0x5d, 0x06, 0x0b, 0xa1, 0xa2, 0x22, 0x95, 0xb5, 0x8c, 0x68, 0xdc, 0xbe];

// ── Task ──
#[derive(Debug, Clone, PartialEq)]
pub struct Task {
    pub id: u64,
    pub title: String,
    pub priority: Priority,
    pub created_at: Timestamp,
    pub permissions: Permissions,
    pub state: TaskState,
    pub _unknown: Vec<u8>,
}

impl vexil_runtime::Pack for Task {
    fn pack(&self, w: &mut vexil_runtime::BitWriter) -> Result<(), vexil_runtime::EncodeError> {
        w.write_leb128(self.id);
        if (self.title).len() as u64 > 256_u64 { return Err(vexil_runtime::EncodeError::LimitExceeded { field: "title", limit: 256_u64, actual: (self.title).len() as u64 }); }
        w.write_string(self.title.as_str());
        w.enter_recursive()?;
        self.priority.pack(w)?;
        w.leave_recursive();
        w.enter_recursive()?;
        self.created_at.pack(w)?;
        w.leave_recursive();
        w.enter_recursive()?;
        self.permissions.pack(w)?;
        w.leave_recursive();
        w.enter_recursive()?;
        self.state.pack(w)?;
        w.leave_recursive();
        w.flush_to_byte_boundary();
        if !self._unknown.is_empty() {
            w.write_raw_bytes(&self._unknown);
        }
        Ok(())
    }
}

impl vexil_runtime::Unpack for Task {
    fn unpack(r: &mut vexil_runtime::BitReader<'_>) -> Result<Self, vexil_runtime::DecodeError> {
        let id_raw = r.read_leb128(10_u8)?;
        let id: u64 = id_raw;
        let title = r.read_string()?;
        if title.len() as u64 > 256_u64 { return Err(vexil_runtime::DecodeError::LimitExceeded { field: "title", limit: 256_u64, actual: title.len() as u64 }); }
        r.enter_recursive()?;
        let priority = vexil_runtime::Unpack::unpack(r)?;
        r.leave_recursive();
        r.enter_recursive()?;
        let created_at = vexil_runtime::Unpack::unpack(r)?;
        r.leave_recursive();
        r.enter_recursive()?;
        let permissions = vexil_runtime::Unpack::unpack(r)?;
        r.leave_recursive();
        r.enter_recursive()?;
        let state = vexil_runtime::Unpack::unpack(r)?;
        r.leave_recursive();
        r.flush_to_byte_boundary();
        let _unknown = Vec::new();
        Ok(Self {
            id,
            title,
            priority,
            created_at,
            permissions,
            state,
            _unknown,
        })
    }
}


// ── TaskList ──
#[derive(Debug, Clone, PartialEq)]
pub struct TaskList {
    pub tasks: Vec<Task>,
    pub _unknown: Vec<u8>,
}

impl vexil_runtime::Pack for TaskList {
    fn pack(&self, w: &mut vexil_runtime::BitWriter) -> Result<(), vexil_runtime::EncodeError> {
        w.write_leb128(self.tasks.len() as u64);
        for item in self.tasks.iter() {
            w.enter_recursive()?;
            item.pack(w)?;
            w.leave_recursive();
        }
        w.flush_to_byte_boundary();
        if !self._unknown.is_empty() {
            w.write_raw_bytes(&self._unknown);
        }
        Ok(())
    }
}

impl vexil_runtime::Unpack for TaskList {
    fn unpack(r: &mut vexil_runtime::BitReader<'_>) -> Result<Self, vexil_runtime::DecodeError> {
        let tasks_len = r.read_leb128(10_u8)? as usize;
        let mut tasks = Vec::with_capacity(tasks_len);
        for _ in 0..tasks_len {
            r.enter_recursive()?;
            let tasks_item = vexil_runtime::Unpack::unpack(r)?;
            r.leave_recursive();
            tasks.push(tasks_item);
        }
        r.flush_to_byte_boundary();
        let _unknown = Vec::new();
        Ok(Self {
            tasks,
            _unknown,
        })
    }
}

